<% content_for :title, "Edit #{@player.name}" %>

<%= link_to "← #{@player.name}", @player, class: "back-link" %>

<div class="page-header">
  <h1>Edit Player</h1>
</div>

<%= render "form", player: @player %>
